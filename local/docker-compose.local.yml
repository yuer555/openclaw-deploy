#==============================================================================
# OpenClaw 本地测试环境 - Docker Compose 配置
# 版本: V3.0（多 Agent 配置化部署）
# 用途: 网络定义，agent 容器使用 config/docker-compose.agents.yml
#==============================================================================

networks:
  openclaw-network:
    driver: bridge
    name: openclaw-network
